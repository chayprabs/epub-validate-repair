# Contributing

1. Install Node 22+, pnpm 10+, Python 3.12+, Docker, Java 17, and Calibre.
2. Run `pnpm install`.
3. Start local services with `docker compose up`.
4. Run `pnpm lint && pnpm typecheck && pnpm test`.
