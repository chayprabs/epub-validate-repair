{
  "name": "epub-validate-repair",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @epubdoctor/web --filter @epubdoctor/shared-types dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
